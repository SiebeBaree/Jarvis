// DesignSystem — Attio-inspired design tokens and reusable components
// (ScoreRing, pace capsule, rep pips, calendar heat-grid).

public enum DesignSystemInfo {
    public static let version = "0.1.0"
}
